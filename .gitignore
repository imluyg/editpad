# ===== Rust 构建产物 =====
# 任意层级的 target/（根目录与 crates/*/target 一并覆盖）
**/target/
**/*.rs.bk

# ===== 日志文件 =====
*.log
logs/

# ===== 环境变量与密钥 =====
.env
.env.*
*.pem
*.key

# ===== IDE / 编辑器 =====
.idea/
.vscode/
*.swp
*.swo

# ===== 操作系统 =====
.DS_Store
Thumbs.db
desktop.ini

# ===== 本地工作文件 =====
# 进度台账仅存本地，不入库
HANDOFF.md
HANDOFF_ARCHIVE.md

# ===== 发布打包产物 =====
# package.ps1 的输出目录（zip 与暂存区），可随时重建
dist/

# ===== 开发辅助资产 =====
# 大文件验收样本等（bench-50mb.log 等），Phase 4 起移入此目录
dev-assets/

# ===== 设计文档 =====
# 本地保留（设计小节、台账、验收清单、体检报告），一律不入库。
#
# ⚠️ 若将来要放单份文档入库，必须写 `docs/*` + `!docs/x.md` 白名单——
# 写 `docs/` 会让否定规则失效：父目录被排除后，子文件无法被重新包含。
docs/

# ===== 平台 / 工具目录 =====
# 代码托管与 CI 平台配置：.github/ 随仓库发布（GitHub Actions 需要），
# 其余平台的本地配置不入库
.gitlab/
.circleci/
*.yml.local

# AI 助手 / 编辑器的工作目录与说明文件：属个人环境，不入库
.claude/
.cursor/
.workbuddy/
.dsh/
.opencode/
.windsurf/
.aider*
CLAUDE.md
AGENTS.md

# 本机临时脚本与转储（点开头的一次性文件）
.hk*.txt
.msgs.txt
.menu.txt
.*_dump.py