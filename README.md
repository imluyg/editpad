# Editpad

轻量文本编辑器，用 Rust 从零实现。目标是带窗口的 GUI 记事本，并且要扛得住大文件——硬指标：

> **50MB 文件秒开、全程不卡。**

## 技术路线

三件事凑齐，大文件就和 5KB 一样流畅：

| 环节 | 方案 | 对应 crate |
|------|------|-----------|
| 存储 | Rope 树状结构，O(log n) 编辑 | `ropey` |
| 加载 | 后台线程流式读取 + 编码检测（UTF-8 / GBK / UTF-16） | `encoding_rs` |
| 渲染 | 视口虚拟化——只为可见的几十行排版和高亮 | 自绘组件（M2） |

架构上 **core 与壳分离**：`crates/core` 是零 GUI 依赖的纯逻辑层（文档模型、编码加载、
原子保存、搜索），`crates/app` 是 iced 界面壳。将来想加终端版，core 直接复用。

## 当前状态：M2b ✅

- [x] 双 crate workspace 骨架
- [x] 打开 / 编辑 / 保存
- [x] 脏标记（标题栏 ● 提示）
- [x] 原子保存（临时文件 + rename）
- [x] 编码自动识别：UTF-8(BOM) / UTF-16LE/BE / UTF-8 / GBK 兜底
- [x] 后台流式加载 + 进度条
- [x] 查找 / 替换、跳转到行、最近文件、快捷键
- [x] **升级 iced 0.14**——解锁系统输入法，中文输入可用
- [x] **自绘虚拟化编辑器**：ropey Document 成为唯一数据源，
      只为可见行排版绘制（`crates/app/src/editor.rs`）
- [x] 鼠标点击定位 / 拖选 / 滚轮滚动；行号栏随行数自适应宽度
- [x] 撤销/重做（rope 结构共享快照，Ctrl+Z / Ctrl+Y）
- [x] 光标导航全家桶：方向键/Home/End/PageUp/PageDown/Ctrl+Home/End，Shift 选区
- [x] **syntect 逐行懒高亮**（M2b）：检查点式状态机支持视口随机访问，
      编辑只失效改动处之后的状态；按扩展名自动选语言，未知类型纯文本快速路径
      （`crates/core/src/highlight.rs`）
- [x] **IME 预编辑串内联显示**（M2b）：组字过程带下划线画在光标处，
      候选框跟随光标矩形
- [x] **CJK 双宽列映射**（M2b）：点击定位、光标、选区、着色片段全部按
      中文=2列计算（P42 起叠加「真实字形布局」：可见行按排版引擎逐字符
      定位，光标/点击/选区与字形零误差，列模型仅作未排版行的兜底）

**遗留打磨项**：平滑滚动、光标闪烁、主题切换、大跳转时检查点补建的异步化。

## 运行

```powershell
cargo run --release
```

大文件验收：打开仓库根目录的 `bench-50mb.log`（68MB / 60 万行），
加载期间界面可拖动、进度条实时推进；打开后滚动与编辑不卡顿。
打开 `.rs`/`.py`/`.md` 等文件可看到语法着色。

## 路线图

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 骨架 + 打开/编辑/保存/脏标记/原子保存 | ✅ 完成 |
| M1 | 日用可用：后台流式加载+进度、查找替换、跳转行、设置与最近文件、快捷键 | ✅ 完成 |
| M2a | 大文件攻坚：iced 0.14（IME 可用）+ 自绘虚拟化编辑器 + rope 数据源 + 撤销重做 | ✅ 完成 |
| M2b | syntect 逐行懒高亮、IME 预编辑内联、CJK 列映射 | ✅ 完成 |
| M3 | 关闭确认、拖拽打开、深色主题+字号设置、dirty 替换保护、打开/另存为死锁修复 | ✅ 完成 |

| M4 | 多标签页（Ctrl+T/W/Tab）、即时保存、JSON 校验格式化、Log/TOML 着色+无扩展名嗅探+MD 预览 | ✅ 完成 |
| M5 | 会话快照：置脏关窗零询问（自动写快照直退）+ 启动恢复上次界面（标签/光标/滚动/未命名内容），异常退出可恢复 | ✅ 完成 |

**遗留项**（非阻塞）：软换行（涉及导航模型重构）、单实例互斥、安装包分发脚本、
平滑滚动/光标闪烁、A± 连点防抖、大字号下行号槽宽系数微调。

## 快捷键

以下为默认组合，均可在 **设置 → 快捷键** 页重映射（点击「修改」按下新
组合键，Esc 取消；冲突会被拒绝；「全部恢复默认」一键还原）。

| 默认组合 | 功能 |
|------|------|
| Ctrl+O / Ctrl+S / Ctrl+A | 打开 / 保存 / 全选 |
| Ctrl+F / Ctrl+G | 查找替换栏 / 跳转到行 |
| Ctrl+Z / Ctrl+Y | 撤销 / 重做 |
| Ctrl+C / Ctrl+X / Ctrl+V | 复制 / 剪切 / 粘贴 |
| Ctrl+D / Ctrl+L | 在下方复制当前行 / 删除当前行 |
| Ctrl+Shift+↑ / Ctrl+Shift+↓ | 当前行上移 / 下移（多行选区整块移动） |
| Ctrl+Shift+U / Ctrl+U | 转为大写 / 小写（有选区只转选区，无选区转全文） |
| Ctrl+Shift+T / L / B | 去除行尾 / 行首 / 行首尾空白（有选区只清触及行，全角空格/NBSP 也算） |
| Ctrl+Shift+S / D / K | 行升序 / 降序排序、去除重复行（有选区只处理触及行，无选区全文；码点序大小写敏感，去重保留首次出现） |
| Ctrl+F2 / F2 / Shift+F2 | 切换当前行书签 / 跳到下一个 / 上一个书签（到文档边缘自动回绕；书签行在行号栏左缘条带显示琥珀圆点） |
| Ctrl+Shift+F2 / Ctrl+Shift+C / Ctrl+Shift+X | 清除全部书签 / 复制全部标记行 / 删除全部标记行（删除可整体撤销，书签一并找回；书签为会话内标注，不写入文件） |
| Ctrl+Shift+M | 跳到配对括号的另一侧（光标邻接 `()` `[]` `{}` 时生效，来回按在两侧往返；两侧括号同步显示下划线提示） |
| Ctrl+Shift+I / O / P | 行首制表符转空格 / 全部制表符转空格 / 行首空格转制表符（有选区只转触及行，无选区全文；制表位与编辑器渲染对齐一致，宽字符占 2 列） |
| Ctrl+Shift+J / H | 合并行（选区触及块合成一行、空行消失；无选区把当前行并入下一行）/ 拆分行（无选区在光标处断行；有选区把选中文本独立成行） |
| Ctrl+Shift+N / R | 删除空行 / 删除空白行（含只含空白字符的行；有选区只清触及块，尾随换行形态保持） |
| Ctrl+Shift+A | 「查找全部」结果面板（列出当前查询的全部命中：行：列 + 行摘录，点击即选中跳转；超大结果集仅显示前 500 条并明示截断） |
| F5 | 在光标处插入当前日期时间（`YYYY-MM-DD HH:MM`，本地时区） |
| Ctrl+Q | 切换行注释（`//` `#` `--` `::` 按语法自动选前缀；有选区只作用触及行，整块已注释则去掉一层） |
| Ctrl+Shift+W | 恢复上次关闭的标签页（会话内记忆最近 10 个命名页；「最近打开」面板置顶同款入口） |
| Alt+Shift+拖拽 | 列块（矩形）选区：松开后输入/退格即逐行替换或删除块内容，Ctrl+C/X 复制/剪切块（各行以换行连接），Esc 取消 |
| Ctrl+Shift+G / Q | 复制完整路径 / 复制文件名（当前页；标签右键菜单同款入口，未命名页提示先保存） |
| Ctrl+T / Ctrl+W / Ctrl+Tab | 新建标签页 / 关闭当前页 / 循环切换标签页 |
| Ctrl+Shift+F | 格式化 JSON（仅 JSON 文件） |
| Ctrl+Home / Ctrl+End | 跳到文档首 / 尾 |
| Shift+滚轮 | 横向滚动（触控板横向分量同样支持） |
| Ctrl+滚轮 | 缩放正文字号（设置弹窗里也可步进调节） |

**标签页右键菜单**：右键点击标签条上的文件名弹出菜单——📌 固定/取消固定
（固定页豁免单页与批量关闭，Ctrl+W 同样拒绝）、保存、另存为/重命名（v1
同一动作兜底）、复制完整路径、复制文件名（命名页专属；快捷键
Ctrl+Shift+G / Ctrl+Shift+Q 作用于当前页）、关闭、关闭其他标签页、关闭
右侧标签页；批量关闭的目标中有未保存改动时弹一次聚合确认，确认后统一
放弃。Esc 或选中单项收起菜单。

**插入日期时间**：F5 在光标处插入 `YYYY-MM-DD HH:MM`（24 小时制本地
时间，日志打点场景），与普通输入一样可撤销。

**菜单栏**：顶部菜单栏「文件 / 编辑 / 查看 / 视图 / 设置」承载全部命令
入口（文件：打开/保存/另存为/最近文件/恢复上次关闭；编辑：剪贴板与编辑
套件/查找跳转/时间戳/行注释；查看：缩放/主题/不可见字符标记；视图：MD
预览/最近文件面板；设置：弹窗入口与备份模式）。展开的浮层位置恒定
（槽位对齐），点击菜单栏其他项直接横移切换，点空白处或 Esc 收起。
无工具栏（第 70 轮用户裁决：与文件菜单重复；查找/替换 = Ctrl+F 或编辑
菜单）。

**状态栏（固定分区）**：最左为文件路径（定宽截断，超长加省略号），竖线
分隔后是常驻统计组「长度 · 行数 · 行 · 列 · 位置」（位置 = 光标全文字
符偏移，1 起；行列均为逻辑行列），中部弹性空白，右竖线分隔后固定
「选区 · 行尾 · 编码」——两条竖线位置恒定，选区出现/消失只更新数字，
任何段落不再推移。行尾/编码仍可点击弹出菜单。

**不可见字符标记（设置→外观）**：「显示空白字符」在空格位置画淡色小
点、制表符画短横；「显示行尾符」在每行末尾画短竖标。纯渲染层叠加，
不改文档内容与光标行为。

**保存时备份（设置→保存）**：覆盖已有文件前把磁盘旧版复制一份——
「覆盖式 name.bak」每次覆盖同目录单个备份；「时间戳历史」写入
`name.bak.d/` 目录按秒留档。超过 64MB 的文件自动跳过；备份失败只在
状态栏提示、绝不阻断保存本身。

**最近文件光标记忆**：重新打开「最近文件」里的文件时，光标与垂直滚动
自动回到上次关闭该页时的位置（每条记忆随文件走，随最近列表上限修剪；
「记住最近文件」关闭时光标记录一并清空，不留隐私痕迹）。

测试策略：core 层纯逻辑全部单测覆盖；随机编辑序列（插入/删除/全部替换/
撤销重做交错，混排三种行尾、CJK 与 emoji）与 String 参照实现逐步对拍
（`core/tests/edit_sequence_fuzz.rs`，固定种子可复现）；编辑器层另有
随机混合操作（插入/移动/选区/大小写/清理/撤销重做）的结构不变量与
「撤销到底回初始、重放到顶终态一致」对拍；50MB 性能内存
用脚本生成的日志文件做回归基准。

## 格式适配（M4）

- 常见代码语言按扩展名自动着色（syntect 内建 + 自维护别名表：
  `log`/`md`/`markdown`/`toml`/`ini`/`cfg`/`conf`/`mk`/`yml` 等）；
- 内嵌迷你语法：**Log**（ERROR/WARN/INFO/DEBUG 级别分色 + 时间戳）、
  **TOML**（节名/键/字符串/注释），随单文件分发；
- 无扩展名文件按内容嗅探：shebang、`<?xml`、JSON/YAML 启发，
  以及 `Dockerfile`/`Makefile` 约定文件名；
- Markdown 文件提供 `MD 预览` 面板（标题/列表/引用/代码块/分隔线/
  粗体斜体行内样式，只读渲染）；
- JSON 文件支持 `Ctrl+Shift+F` 一键校验并格式化（错误定位到行列）。

## 会话快照与启动恢复（M5）

关闭窗口时有未保存改动**不再弹确认**：全部置脏页（含未命名页）自动写入
快照区 `%APPDATA%\editpad\snapshot\` 后直接退出——「要不要保存」推迟到
下次启动再问。写盘采用先页文件后清单的 write-ahead 顺序，任何时刻断电/
崩溃都不会出现半截会话；快照总量超 64MB 按最旧优先淘汰。
确认条「放弃更改」= 连快照一起丢。

**再次打开 = 上次的界面**：启动时按会话清单静默重建全部标签页——
未保存的内容从快照原样回来（保持置脏），干净文件自动重新加载，
标签顺序、激活页、每页光标与滚动位置一并还原；未命名页编号延续
（新页不会重名）。若上次是异常退出（清单缺正常收尾标记），会弹一次性
「检测到未保存的工作区」提示，由你选择恢复或丢弃。恢复总量受内存护栏
（256MB）约束，超出时优先保留激活页并在状态栏汇总。

**运行中心跳兜底**：编辑过程中每隔 `snapshot_interval_secs` 秒（默认 10，
允许 5~120）巡检一次置脏页，只把内容有变化的页增量写进快照区——
打字中途崩溃/杀进程，重启至多丢一个间隔的输入；未命名页同样覆盖
（不自动写盘时的兜底）。超过 16MB 的大文档不参与心跳重写（只在退出时写）；
开启自动写盘且落盘成功清脏后，下一拍会把该页从快照清单中转为已落盘状态。
心跳写的中间清单不带「正常收尾」标记，正是异常退出检测的判据。

隐私开关（`%APPDATA%\editpad\config.toml`）：

- `enable_snapshots = false`——完全关闭（启动时清空存量快照区）；
- `remember_session = false`——不恢复界面、退出也不写清单
  （启动时清空存量会话）；
- `exit_mode = "ask"`——恢复旧的「每次询问确认条」行为。

以上开关连同主题/字号/编辑后自动写盘（默认关闭：修改留在窗口内，
Ctrl+S 才写原文件，对标主流编辑器；开启后若文件被外部改动会先拒写并
提示；延迟秒数可调）都收编在工具栏「设置」弹窗里，改动即时写回
`config.toml`，无需手改文件；弹窗尾部附只读热键速查表（busy 加载/保存
期间不可打开，Esc 关闭）。

**便携模式（P101，多份部署隔离）**：常规情况下 `config.toml` 与会话
快照都存系统目录（`%APPDATA%\editpad\`）——**与 exe 放在哪里无关**，
任何拷贝的程序都共享同一份用户数据（设置、最近文件、未保存会话互相
可见）。需要相互隔离的部署（如一份稳定生产版 + 一份开发版）：在 exe
**同目录**放一个空文件 `portable.txt` 即可启用便携模式——配置与会话
快照改存 exe 目录内，该拷贝自成一体，与其他拷贝/系统目录互不读写。
打包脚本 `package.ps1 -Portable` 会直接在发布目录里放下标记文件。

**编码与行尾（状态栏可点）**：状态栏右侧显示当前编码与行尾，两者都是
可点标签。编码菜单可选「以 UTF-8 / UTF-8(BOM) / GBK 保存」——选择后
本页记住该偏好（此后每次保存沿用，重新载入或另存为新路径时重置）；
GBK 无法表示的字符（如 emoji）按 `&#编号;` 数值实体写入并明确提示。
转码不可逆（如 GBK→UTF-8、去 BOM）时状态栏都会说明，知情权与 P6 一致。
行尾菜单显示当前主导行尾，可一键转换为 CRLF（Windows）或 LF（Unix），
混合行尾一并归一；转换是普通文档编辑，可撤销、参与自动保存与快照。
未命名页需先「另存为」取得路径后才能选择保存编码。

**正文字体选择**：设置弹窗里可从系统已安装字体中按关键字过滤并选定
正文与 UI 的字形族（数据来自启动期的系统字体清单），「回退默认」恢复
内置等宽字体。配置按族名写入 `font_family`；所选字体未安装时本次启动
自动回退默认并在状态栏提示一次，不抹掉配置（重装后自动恢复）。
建议选含中文字形的等宽字体（如更纱黑体、Noto Sans Mono CJK SC、
新宋体）；非等宽字体的列对齐观感不整齐，但光标/点击/选区仍按真实字形
位置贴合（P42 根治后不再产生定位漂移）。

## License

Licensed under `Apache-2.0`（见 `LICENSE-APACHE`），
与 Rust 生态惯例一致。
