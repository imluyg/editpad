# 格式纪律：当前保持 rustfmt 默认风格（仅显式写出 max_width）。
# 注：2026-09-25 已用 rustfmt 全量收敛（core 203 hunk / app 552 hunk 两批独立
# 提交），`cargo fmt --all -- --check` 本机实测 0 偏差、CI 的 fmt 作业已转阻断。
# 新代码提交前本地跑一遍 `cargo fmt --all` 即可保持清零。
max_width = 100