# 提交与协作约定

> 适用本仓库（个人项目，单线 `main`）。目标只有两条：**`git log` 一眼能读**、
> **每个提交能独立回滚**。约定从简，不引入形式化规范（无 `feat:`/`fix:` 前缀、
> 无语义化版本推导）——个人项目用不上它们的收益，只增加负担。

## 1. 提交信息

### 首行（主题）

- **≤ 50 字，一句话说清「改了什么」**；
- **不加编号前缀**：不写 `P#`、不写「第 N 轮」、不写 `feat:`/`fix:`；
- 不以句号结尾；不用「更新」「修一下」「杂项」这类零信息表述；
- 中文首行即可；若将来面向英文读者，首行改英文、正文保持中文。

### 正文（需要时）

空行之后，固定四段小标题，每段一到几句话。**首行只留结论，证据全部下沉到正文**
——历史上曾把整篇轮次日志塞进首行（最长 1163 字符），`git log --oneline` 与
仓库网页列表都读不了。

```
问题：用户能看到的现象（含复现条件）
原因：根因一句话 + 关键证据（哪个文件/哪个假设破了）
改法：动了哪几处、已知取舍或局限
验证：回归 N 条（说清测什么）+ 全量数字 + clippy 结果
```

**示例**

改前（首行 1163 字符，节选）：

```
字体一致性——方案 c 落地+UI 口径统一。调研结论(逐层核实 vendored 源码):iced 0.14 的
Font::MONOSPACE …
```

改后：

```
正文与 UI 统一钉到 CJK 等宽字体，列宽按实测字形校准

问题：等宽解析落到不含 CJK 的字体上，中文行宽与光标位置逐字漂移。
原因：Family::Monospace 的解析目标由系统默认决定，未覆盖 CJK 等宽候选。
改法：启动期按候选表把 Monospace 钉到首个已安装的 CJK 等宽字体；列宽改实测 advance。
验证：字体解析单测 + 像素级列宽对齐回归；全量测试全绿，clippy 零警告。
```

## 2. 粒度

一个提交 = **一个可独立回滚的变更**，判据是「能否用一句话说清」。

| 情形 | 做法 |
|---|---|
| 同一根因的跨文件同修（实现 + 测试 + 文档） | **合成一条**（拆开会让回滚半途而废） |
| 一轮里互不相干的几件事（如「浮层改版 + 行号裁字 + 输入法焦点」） | **分开提**（回滚其一不牵连其他） |
| 纯格式化 / 纯文档 / 依赖升级 | 各自独立一条，便于 `git blame` 跳过错噪提交 |
| 「先保存一下」的中间态 | 不要提；本地 `stash` 或补完再提 |

## 3. 提交前检查

```powershell
& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --workspace     # 必须全绿
& "$env:USERPROFILE\.cargo\bin\cargo.exe" clippy --workspace --all-targets -- -D warnings
```

- 只暂存本提交相关的文件：**显式写路径 `git add <file>`，禁止 `git add -A`**
  （避免把 `dist/`、日志、临时探针一起带进来）；
- 回归测试至少一条，优先放 core 层。

## 4. 历史与改写

- **未推送**：随意 `commit --amend` / `rebase` / 改提交信息，改完自检
  `git diff <旧> <新>` 应为空（只改信息、不动内容）；
- **已推送**：不改写、不 force push；要更正就在后面补一条说明；
- **内部台账（HANDOFF，本地不入库）入账即冻结**：台账按提交哈希登记，一旦登记
  就当冻结，避免「改写完再回头同步台账」的往返。

## 5. 发布记录

- 打包发布时打 **tag**（`v0.1.0`），release note 从该区间的提交首行摘取；
- `dist/` 里 exe 附 SHA256，与 tag 一起构成可追溯的发行记录；
- 版本节点的表达放在 tag/release note，**不要塞进提交信息**。

## 6. 文案底线

对外可见的一切内容（README、代码注释、提交信息、设置文案、热键描述）使用中性
表述（如「同类编辑器」「主流编辑器」），**不点名其他软件、不引用其界面文案或
命名**。
